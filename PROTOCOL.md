# 输入输出规范

本文定义 DDR-AI 沙箱里**所有跨越边界的接口**。一共四条边界，从内到外：

```
①  客户程序  ↔  CPU          虚拟指令集（寄存器、内存、OUT/HALT）
②  CPU       ↔  沙箱           load(addr) / store(addr, val) 两个回调
③  沙箱      ↔  内存条         严格指令协议（READ / WRITE / ERASE）   ← 核心
④  沙箱      ↔  黑窗           事件流（总线指令、回复、故障、输入请求）
```

规范与实现一一对应，改代码必须同步改这里。判别"哪些是合规的"的**唯一权威**
是 `src/ddr_ai/ai_backend.py` 的 `decode_hex()` / `cmd_*()`，本文只是它的文字版。

---

## ③ 沙箱 ↔ 内存条：严格指令协议

这是本项目最关键的一条边界：**沙箱只发指令，内存条只回数据，双方都不许说废话。**

### 3.1 请求（沙箱 → 内存条）

只有三种指令，全部是单行文本，字段之间用**恰好一个 ASCII 空格**分隔。

```
READ  <地址> <字节数>
WRITE <地址> <字节数> <十六进制数据>
ERASE <页号>
```

| 字段 | 语法 | 说明 |
|---|---|---|
| `<地址>` | `0x` + 定宽小写十六进制 | 宽度见 3.2；一律小写 |
| `<字节数>` | 十进制正整数 | 不加前导零，不带 `+` |
| `<十六进制数据>` | 连写小写十六进制 | 长度必须恰好 `<字节数>`×2 个字符 |
| `<页号>` | 十进制非负整数 | 页大小由启动器决定 |

实例：

```
READ 0x0010 1
READ 0x0010 4
WRITE 0x0010 1 2a
WRITE 0x0010 4 0000002a
ERASE 0
```

**当前实现只发单字节访问**（`<字节数>` 恒为 `1`），因为 `sort.asm` 之类的
客户程序是逐字节读写内存的。协议本身支持多字节，留给将来的 `MEMCPY` 之类。

### 3.2 地址格式

宽度**按容量自适应**，最少 4 位，取偶数位：

| 容量 | 地址宽度 | 起始地址 | 末尾地址 |
|---|---|---|---|
| 256 B | 4 | `0x0000` | `0x00ff` |
| 4 KiB | 4 | `0x0000` | `0x0fff` |
| 64 KiB | 4 | `0x0000` | `0xffff` |
| 1 MiB | 6 | `0x000000` | `0x0fffff` |

规则：宽度 = `max(4, len(hex(容量-1)))`，若为奇数则加一。
实现见 `ai_backend._fit_addr_width()`。宽度由沙箱按容量算一次，
三种指令共用同一个宽度。

### 3.3 回复（内存条 → 沙箱）

回复**必须只有一行**，不许解释、道歉、拒绝、加代码块、加标点、加 emoji。

| 请求 | 合规回复 | 说明 |
|---|---|---|
| `READ` | 连写十六进制字节 | 解码后长度必须**恰好等于**请求的字节数 |
| `WRITE` | `OK` + 一个空格 + 连写十六进制 | 表示"我记下的是这些" |
| `ERASE` | `OK` | 无附加内容 |

实例：

```
请求: READ 0x0010 4           回复: 0000002a
请求: WRITE 0x0010 4 0000002a 回复: OK 0000002a
请求: ERASE 0                 回复: OK
```

### 3.3b 上下文注入（真模型专用）

语言模型是**无状态**的：每次请求独立，它不记得上一次写过什么。
只发 `READ 0x0040 1` 的话，它没有任何信息可依，只能瞎猜。

所以真模型后端默认走 `state` 模式，把内存内容随请求发过去：

```
【当前内存内容】
0040: 48 49 00 00 00 00 00 00 00 00 00 00 00 00 00 00  HI..............

上表是内存的权威内容。READ 必须照表回答，WRITE 必须回显你写入后的值。

【本次指令】
READ 0x0040 1
```

| 模式 | 内容 | 效果 |
|---|---|---|
| `state` | 十六进制转储 + 指令 | 读得对（默认） |
| `recent` | 最近 16 条收发记录 + 指令 | 省 token，可能错 |
| `none` | 裸指令 | **读全错**（复现无上下文灾难） |

边界：

- **只对真模型生效**。本地仿 AI 直接从介质读，真人是在看屏幕，都不需要。
- 转储大小受 `context_max_bytes`（默认 2048）限制，超出只发焦点附近的节选并注明。
- 注入上下文**不改变解析规则**：回复仍按 3.4/3.5 校验，
  模型照样可能给出与转储不符的答案——那仍是故障。

### 3.3c 思考参数（真模型专用）

**关掉思考 ≠ 模型就不思考了。** `deepseek-flash` 这类模型关不掉思考，
不发参数它照样想、照样吃 `max_tokens`，表现为"跑着跑着力不从心"。

所以每个思考字段配一个独立开关，决定"请求 JSON 里要不要出现它"。
硬编码字段名必然在某家模型上报"未知字段"，因此全部可选：

| 开关 | 字段 | 备注 |
|---|---|---|
| 思考总开关 | — | 关闭时改发「关闭思考 JSON」（默认不发任何字段） |
| 思考强度 | `reasoning_effort` | OpenAI 系 `low`/`medium`/`high`；档位名可手改 |
| 思考预算 | `thinking_budget_tokens` | **Anthropic 会改发 `thinking.budget_tokens`**，因为它不认识顶层字段 |
| 自定义思考参数 | 任意 JSON | 适配各家不同命名 |

优先级（后者覆盖前者）：

1. 内置基础字段（`model` / `temperature` / `max_tokens` / `messages`）
2. 思考参数
3. `extra_json`（**最高**，可覆盖上面任何一项）

非法 JSON 一律抛 `BackendError`，不静默吞掉。

运行结束后汇总会报出思考开销（思考 token 总数、次数、均值），
并提示它"吃 max_tokens 但不产生回复"——这是排查力不从心的直接证据。

### 3.4 解码算法

沙箱对 `READ` 的回复、以及 `WRITE` 回复中 `OK ` 之后的部分，都走同一个解码器。
步骤严格按顺序：

1. 若输入为 `None` → 失败
2. 去掉首尾空白；若只剩空串 → 失败
3. 删除所有 `0x` / `0X`（大小写不敏感）
4. 删除所有空白、`,`, `:`, `_`, `-`
5. 若为空 → 失败
6. 若长度为奇数 → 失败
7. 若不匹配 `^[0-9a-fA-F]+$` → 失败
8. 成功，返回 `bytes.fromhex(...)`

**该算法只做容错，不做补全**：它不会截断、不会补零、不会猜。
解析不出干净 token 就是失败。

### 3.5 合规判定（关键）

解码成功 ≠ 合规。**字节数必须精确匹配**，多一个少一个都算故障。

| 阶段 | 判定 |
|---|---|
| `READ` | 解码成功 **且** `len(reported) == nbytes`，否则记**读不符** |
| `WRITE` | 剥掉 `OK ` 前缀后解码，成功 **且** `len(payload) == len(请求数据)`，否则记**写坏** |
| `ERASE` | 不校验内容（回复仅作为原文记录） |

故障处理有一条铁律：

> **以内存条的回复为准。它报什么，就是什么；报错了也不替它改。**

- 写：它说记下了 `ff`，纸上的就是 `ff`，哪怕指令要求 `2a`。
- 读：它说读到 `ff`，客户程序拿到的就是 `ff`，哪怕纸上写着 `2a`。
- 不重试、不用纸上内容覆盖、不替它补位数。

沙箱**只报错、不纠正**——纠正了就没有"幻觉"这回事了。
报错只是通知你出事了，不是一种恢复机制。

唯一不成立的情况是**它根本没给内容**（后端异常或空回复）：
没有内容就无法"以它为准"，此时读按 `0` 返回、写直接丢弃，并各记一次故障。
这不是纠错，这是无内容可依据时的既定行为。

### 3.6 合规与不合规示例

以下全部经过 `tests/test_protocol.py` 验证。

| 回复原文 | 解码结果 | 判定 |
|---|---|---|
| `0000002a` | `0000002a` | 合规 |
| `00 00 00 2a` | `0000002a` | 合规（容忍空格） |
| `0x0000002a` | `0000002a` | 合规（容忍 0x） |
| `00-00-00-2a` | `0000002a` | 合规（容忍连字符） |
| `0000002` | 失败 | 奇数长度 |
| `zzzz` | 失败 | 非十六进制 |
| `OK 0000002a`（作为 READ 回复） | 失败 | 带了不该有的前缀 |
| `已记下 00 00 00 2a` | 失败 | 夹带中文 |
| ``（空） | 失败 | 空回复 |
| `00002a`（请求 4 字节） | 解码成功但长度 3 | **读数不合规** |
| `OK 00002a`（请求 4 字节） | 解码成功但长度 3 | **写数不合规** |
| `OK`（WRITE 回复） | 失败 | 没有数据 |

注意"失败"与"不合规"是两件事：**解码失败**意味着拿不到字节；
**长度不合规**意味着拿到了字节但位数不对。两者都记故障，
但后续行为不同——前者无内容可依据，后者照样以它为准。

### 3.6b 思考型模型的回退路径

真模型（尤其思考型）有四种不守规矩的方式，依次兜住：

| 现象 | 处理 | 是否记故障 |
|---|---|---|
| `content` 为空、答案在 `reasoning_content` | 自动改读 `reasoning_content` | 否 |
| 短推理里夹着答案（"让我看看……2a"） | 从**结尾**捞出十六进制（**仅回退路径**） | 否 |
| 想完了没力气回答（`finish_reason=length`） | 升档到 4 倍 `max_tokens`、禁止思考后重试 | 否（升档成功时） |
| **把整段思考当正文吐出来** | 判定不可用，升档重试 | 否（升档成功时） |

#### 严格可用闸门（`_strict_usable`）

这是防止"思考泄漏被当成答案"的关键。判定规则：

- **严格路径**（`content` 字段）：整条回复必须恰好是 `want_bytes` 个字节的
  十六进制（允许 `OK` 前缀与空格/`0x` 等手滑）。任何多余内容一律判不可用。
- **回退路径**（`reasoning_content`，`allow_salvage=True`）：允许夹带思考文字，
  但必须同时满足：
  - 总长度 ≤ 40 字符
  - 无换行
  - **结尾是一个干净的十六进制 token**，长度 ≥ `want_bytes*2`

最后一条是确定判据，不是启发式：模型习惯先分析、最后给结果，
所以答案在结尾。从推演正文中间捞十六进制等于凭空捏造数据——
曾经真的捞出来一个 `ab`。

`salvage_hex` 也按同一原则重写：**结尾 token 优先**，中间的只作最后手段。

#### 上下文只能有一份权威

`state` 模式下**不再附带【最近的操作】**。

之前两份都给，模型必须在"权威表"和"操作流水"之间自己调和，
结果它开始怀疑表是旧的、流水是新的，然后**虚构出第三份内存内容**开始自我辩论。
权威的东西只能有一份。

升档重试会在黑窗打一条 `fyi` 提示（每次运行最多一条），因为
静默恢复虽然不报错，但**延迟翻倍**这件事用户有权知道。

### 3.6c 思考泄漏的诊断

黑窗报错里会明确写出来：

```
[!!] 读数不合规：请求 1 字节，它给了 (空)。
     （思考泄漏，已升档到 max_tokens=2048 并禁止思考后重试。
      它把思考过程当正文吐出来了，建议关掉思考或调小 max_tokens）
```

看到"思考泄漏"就说明该动「思考控制」面板了。

### 3.7 故障计数

两个计数器，**互不混淆**：

| 计数器 | 口径 |
|---|---|
| `stats.faults` | **写侧故障**：回复无内容、写数不合规、或写入内容与指令不符 |
| `stats.read_mismatches` | **读侧故障**：回复无内容、读数不合规、或读数与纸上所记不符 |
| `Sandbox.hallucinations` | 上面两者之和（"内存条不老实"的总次数） |

后端抛 `BackendError`（无响应、超时、HTTP 错误、JSON 非法）时：
读按 `0` 返回、写直接丢弃，各计入对应故障，客户程序继续跑，**沙箱不崩**。

---

## ③b 能工智人 · 真实模式：人机输入

此时"内存条"是人，协议不变，只是回复由你在界面上敲。

### 3b.1 交互循环

```
沙箱线程                           UI 线程
   │                                  │
   ├─ ask(cmd) ─→ push_control ──────→│ 输入栏 enable，显示 cmd
   │            （阻塞在队列上）      │
   │                                  │ 你敲入文本 → 提交
   │←────────── queue.put(text) ──────┤
   │                                  │
   ├─ 按 3.4/3.5 校验你的输入          │
```

- **提示文本**就是原始指令本身（如 `READ 0x0040 1`），不做任何包装。
- **输入格式**与 3.3 完全一致，你在替内存条说话。
- **超时**：600 秒无人回答 → 当作空回复，记为一次故障。
- **点中止**：UI 会补一个空回复把沙箱放出来，然后沙箱在下一次内存访问时退出。

### 3b.2 输入示例

| 输入栏提示 | 你应当输入 |
|---|---|
| `READ 0x0040 1` | `00`（或任意 2 位十六进制） |
| `WRITE 0x0040 1 48` | `OK 48` |
| `ERASE 0` | `OK` |

敲错也会被如实记成故障——**你就是内存条，你要负责**。
沙箱不会替你补正，也不会用纸上的内容覆盖你给的答复。

---

## ④ 沙箱 ↔ 黑窗：事件流

沙箱把过程写成事件流，UI 定时取走渲染。每条事件是 `(kind, text)`。

### 4.1 事件类型与配色

| kind | 配色 | 用途 |
|---|---|---|
| `bus` | 青 `#4fc3f7` | 沙箱发出的指令，前缀 `>> ` |
| `ai` | 白 `#e8e8e8` | 内存条的回复，前缀 `<< ` |
| `sys` | 灰 `#8d8d8d` | 配置、汇总、内存转储 |
| `io` | 绿 `#a5d6a7` | **程序实时输出**，前缀 `   程序输出 ▸ ` |
| `err` | 红 `#ff5252` | 故障：无响应、读数/写数不合规、读错、写错 |
| `ask` | 黄 `#ffd54f` | 真实模式下等待你输入 |
| `fyi` | 暗灰 `#6d6d6d` | 附注：**升档重试**提示（每条运行最多一次） |

### 4.2 行格式

```
>> READ 0x000a 1                     总线指令（原文，未加工）
<< 05                                内存条回复（原文，未加工）
[!!] 写错了：0x000a 它记成 fd，指令要求 05。   故障通知
```

`>>` 与 `<<` 后面的内容**是协议的原文**，没有额外包装——方便直接比对。
正常路径下**没有任何附注行**：发一条指令、收一条回复，如此而已。

### 4.3 故障消息

故障只报"它错了"，**不附带纠正动作**：

| 场景 | 消息 |
|---|---|
| 后端抛异常（读） | `[!!] 内存条无响应：…；该次读按 0 返回。` |
| 后端抛异常（写） | `[!!] 内存条无响应：…；该次写丢弃。` |
| 读回复字节数不对 | `[!!] 读数不合规：请求 1 字节，它给了 …。` |
| 写回复字节数不对 | `[!!] 写数不合规：指令给 1 字节，它回了 …。` |
| 读到的不等于纸上记的 | `[!!] 读错了：0x… 它说 …，实际记的是 …。以它说的为准。` |
| 写入的不等于指令要求的 | `[!!] 写错了：0x… 它记成 …，指令要求 …。` |

### 4.4 控制消息

除了渲染事件，队列里还有两类控制消息，由 UI 线程派发：

| 消息 | 含义 |
|---|---|
| `RunResult` | 本次运行结束，携带全部指标 |
| `HumanPrompt(cmd)` | 真实模式：请就这条指令给出回复 |

设计约束：**沙箱线程绝不直接碰 tkinter**（连 `root.after()` 都不行），
一切跨线程通信都走这一个队列。

---

## ① 客户程序 ↔ CPU：虚拟指令集

客户程序是本项目自造的汇编（`programs/*.asm`）。字长 **8 位无符号**，
加减法按 256 取模，没有符号位。

### 1.1 指令表（17 条）

| 指令 | 语义 | 内存访问 |
|---|---|---|
| `MOVI r, imm` | `r = imm & 0xFF` | — |
| `LOAD r, addr` | `r = mem[addr]` | 读 1 B |
| `LDI r, ra` | `r = mem[ra]`（间接） | 读 1 B |
| `STORE addr, r` | `mem[addr] = r` | 写 1 B |
| `STI ra, r` | `mem[ra] = r`（间接） | 写 1 B |
| `SWAP a1, a2` | 交换两地址内容 | 读 2 + 写 2 |
| `ADDI r, imm` | `r = (r + imm) & 0xFF` | — |
| `ADD rd, rs` | `rd = (rd + rs) & 0xFF` | — |
| `SUBI r, imm` | `r = (r - imm) & 0xFF` | — |
| `SUB rd, rs` | `rd = (rd - rs) & 0xFF` | — |
| `JMP label` | 无条件跳转 | — |
| `JZ r, label` | `r == 0` 则跳转 | — |
| `JNZ r, label` | `r != 0` 则跳转 | — |
| `JGT r, imm, label` | 无符号 `r > imm` 则跳转 | — |
| `JLT r, imm, label` | 无符号 `r < imm` 则跳转 | — |
| `OUT r` | 把 `r` 追加到程序输出 | — |
| `HALT [code]` | 停机 | — |

寄存器：`r0` `r1` `r2` `r3`，初值全 0，**故意只给 4 个**——
寄存器越少，程序越被迫频繁访问内存，黑窗越热闹。

### 1.2 汇编语法

```
; 分号或 # 开始的行尾注释会被忽略
label:   MOVI r0, 1        ; 标签独占一段冒号前缀
         .data 10 5 3 9 1  ; 数据段：起始地址后跟若干字节
```

跳转目标可以是标签，也可以是十进制字面量（当前 PC 的绝对下标）。

### 1.3 程序输出

- `OUT r` 把寄存器当 ASCII 打出去（**不产生内存访问**，所以它不慢）。
- 输出是**交互式实时**的：每产出一个字节，黑窗立刻以 `io` 事件刷出来，
  不等程序跑完。这样你能看到"读一个字节 → 打一个字符"的因果顺序。
- 刷新策略：遇到换行立刻刷；否则攒够 16 字节刷一次；程序结束时兜底刷尾巴。
- 空的输出不打印，只在收尾时说明"本程序没有产生输出"。

**收尾报告里不再重复程序输出**（`[程序输出]` 那行已删除）——
同一份输出出现两遍会让人以为是两次不同的结果。

字节的渲染格式由启动器选择：

| 格式 | 4 字节 `[1,3,5,9]` 的呈现 |
|---|---|
| `ascii` | 可打印字符原样，其余转义成 `\xNN` |
| `dec` | `1 3 5 9` |
| `hex` | `01 03 05 09` |

### 1.4 异常

| 异常 | 触发条件 | 沙箱表现 |
|---|---|---|
| `GuestError` | 未知指令 / 未知标签 / 参数不足或非法 / 步数超 `max_steps`（100000） | 运行失败，不崩溃 |
| `MemoryError_` | 地址越界（`addr < 0` 或 `addr + len > 容量`） | 运行失败，记一次 `stats.faults` |

---

## ② CPU ↔ 沙箱：内存回调

沙箱实现 `guest.MemoryPort` 协议，这是 CPU 唯一的对外通道：

```python
def load(self, addr: int) -> int:      # 返回 0..255
def store(self, addr: int, value: int) -> None
```

- 每次调用**恰好产生一次协议交互**（一条指令 + 一条回复），不重试、不拆分。
- 返回值一律取自内存条的回复，**不取自纸**。纸只用于判断它对不对。
- 用户点中止后，下一次调用抛 `GuestError("用户中止了运行")`。
- `GuestProgram` 的 `.data` 会在 CPU 构造时通过 `store` 预置——
  也就是说**初始化数据也要走完整的内存条流程**，一样会被 AI 记错。

---

## ⑤ 执行结果（RunResult）

一次运行的完整产出：

| 字段 | 类型 | 含义 |
|---|---|---|
| `ok` | bool | 是否正常停机 |
| `output` | bytes | `OUT` 累积的程序输出 |
| `steps` | int | CPU 执行的指令数 |
| `program` | str | 程序名 |
| `message` | str | 结论（正常停机 / 内存错误 / …） |
| `sim_seconds` | float | 仿真世界的耗时 |
| `real_seconds` | float | 现实世界的耗时 |
| `accesses` | int | 总线访问次数 |
| `faults` | int | 写坏 + 读不符（= `hallucinations`） |
| `effective_bps` | float | 等效读写速率（bit/s） |
| `memory` | PaperMemory | 介质对象，可取完整指标与转储 |

---

## ⑥ 边界与限制

| 项目 | 值 | 备注 |
|---|---|---|
| 内存容量 | 256 B ~ 1 MiB | 启动器下拉框 |
| 页大小 | 32 B ~ 1 KiB | 必须 ≤ 容量 |
| 存储字长 | 8 位无符号 | 全链路统一 |
| 寄存器 | 4 个（r0..r3） | 8 位 |
| 单条指令最多访问 | 4 次（`SWAP`） | 每次访问一次协议往返 |
| 客户程序步数上限 | 100000 | 超出报死循环 |
| 单次访问节流上限 | 0.35 s | `pace_cap`，防止一等等三分钟 |
| 人工输入超时 | 600 s | 超时按空回复处理 |
| LLM 请求超时 | 30 s（可配） | 失败按无响应处理 |
| LLM 最大输出 | 64 token（可配） | 够回一行十六进制 |

### 明确不做的事

- **不压缩**：协议层不做任何打包，一字节就是一次往返。
- **不缓存**：不存在的"内存条缓存"——那会掩盖延迟。
- **不纠错**：内存条报什么就是什么。沙箱会**检测并报错**（比对纸上记录），
  但不重试、不覆盖、不补位、不修复——报错只是通知，不是恢复。
- **不并发**：一条指令必须收到回复才会发下一条，全程串行。
